# -*- mode: snippet -*-
# name: definition
# uuid: definition
# key: def
# --
\begin{definition}${1:$(when (> (length yas-text) 0) "[")
}${1:name}${1:$(when (> (length yas-text) 0) "]")
}${2:$(when (> (length yas-text) 1) " \\\\label{")
}${2: $(unless yas/modified-p (yas-auto-next (reftex-label "definition" 'dont-insert)))}${2:$(when (> (length yas-text) 1) "}")
}
  $3
\end{definition}
$0